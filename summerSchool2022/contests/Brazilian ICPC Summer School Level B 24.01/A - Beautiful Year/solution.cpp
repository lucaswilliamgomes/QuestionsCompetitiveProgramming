#include <bits/stdc++.h>

using namespace std;

int main() {
    int year;
    cin >> year;
    for (int i = year; i < 9000; i++) {
        string a = to_string(i);
        if (a[0] != a[1] && a[0] != a[2] && a[0] != a[3])
    }
}