#include <bits/stdc++.h>

using namespace std;

int isPossible(int sacosPipoca, int quantCompetidores, int maxPipoca, vector<int> &quantPipocas) {

}

int main () {
  int sacosPipoca, quantCompetidores, maxPipoca;
  cin >> sacosPipoca >> quantCompetidores >> maxPipoca;
  vector<int> quatPipocas(sacosPipoca);

  for (int i = 0; i < sacosPipoca; i++) {
    cin >> quatPipocas[i];
  }


}