#include <bits/stdc++.h>

using namespace std;

int main (){
    int otavio, novo, velho;

    cin >> novo >> otavio;
    velho = otavio + (otavio - novo);
    cout << velho << endl;
    
    return 0;
}