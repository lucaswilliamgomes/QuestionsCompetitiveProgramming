#include <bits/stdc++.h>

using namespace std;

int main () {
    int t;
    cin >> t;
    while (t--) {
        int n;
        cin >> n;
        vector<int> ans;
        if (n >= 4) {
            if (n % 2 == 0) {
                
            } else {

            }
        } else {
            cout << "-1" << endl;
        }
    }

    return 0;
}